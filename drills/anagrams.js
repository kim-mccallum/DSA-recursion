const word = "quit";

// string, an empty array to hold results
function anagrams(string, output = []) {
  // split the string into an array
  // base case - length of two
  if (string.length === 2) {
    // swap the letters
    //
    // return the letters swapped and joined and pushed to the array
  }
  // recursive case
  if (string.length > 2) {
    // call anagrams(string.pop())
  }
}

// general steps
// get it working
// isolate first later, swap two, move down the line????
// split the string into an array
// create and empty results array
// loop over the character array - outer loop to hold a letter steady
// loop to take care of the other letters
// make it pretty
// optimize
